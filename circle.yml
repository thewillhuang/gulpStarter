machine:
  services:
    - docker
dependencies:
  override:
    - docker info
    - docker build -t gulpStarter:$CIRCLE_SHA1 .
    - docker run gulpStarter:$CIRCLE_SHA1 npm run prod
test:
  override:
    - docker run gulpStarter:$CIRCLE_SHA1 npm run test;
